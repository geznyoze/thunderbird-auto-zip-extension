# thunderbird-auto-zip-extension
A simple and lightweight Thunderbird extension that prompts you to zip attachments if their total size exceeds a configurable limit.
To use it, simply import the .xpi file into Thunderbird. To configure the limit, use the extensions options menu.